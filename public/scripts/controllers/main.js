'use strict';

/**
 * @ngdoc function
 * @name kakesSampleApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the kakesSampleApp
 */
angular.module('kakesSampleApp')
  .controller('MainCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
